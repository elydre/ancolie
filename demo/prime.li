:n

func is_prime(n) {
    :i

    if n 3 % 0 == {
        return
    }

    i = 5
    while i i * n 1 + < {
        if n i % 0 == {
            return
        }
        if n i 2 + % 0 == {
            return
        }
        i = i 6 +
    }

    dump(n)
}

dump(2)
dump(3)

n = 5

while n 65000 < {
    is_prime(n)
    n = n 2 +

    is_prime(n)
    n = n 4 +
}
